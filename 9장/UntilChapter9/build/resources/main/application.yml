spring:
  jpa:
    show-sql: true
    hibernate:
      format-sql: true

    defer-datasource-initialization: true

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa

  h2:
    console:
      enabled: true

# 토큰 발급자 및 비밀키 설정
jwt:
  issuer: testing@gmail.com
  secret_key: 1234qwerty
